CREATE TABLE `booking`.`test` (
                                    `id` INT NOT NULL,
                                    `nume` VARCHAR(45) NULL,
                                    PRIMARY KEY (`id`)
                                );